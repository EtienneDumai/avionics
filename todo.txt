TODO - Prochaines etapes physique
==================================

1. [FAIT] Gravite
   - g (9.81 m/s2) en static const dans Simulator, setVerticalSpeed() dans AirplaneState,
     computeVerticalSpeed() appelee dans simLoop()

2. [FAIT] Portance (lift)
   - 0.5 * RHO * v_air^2 * Cl * S, Cl constant (_liftCoef), integree dans computeVerticalSpeed()
     (force nette verticale = lift - poids)
   - Simplification toujours en place : portance purement verticale (pas de tangage/roulis)

3. [FAIT] AOA (_AOA) et Cl dynamique
   - AirplaneState::computeAOA(Vec3 forwardVec, Vec3 velocityVec) ajoutee (meme pattern que
     computeIAS : void, parametres externes plutot que lecture interne, pour la testabilite)
   - Vecteur vitesse reconstruit dans Simulator::simLoop() a partir des memes morceaux que
     l'integration de position : (forward.getX()*groundSpeed, forward.getY()*groundSpeed, verticalSpeed)
   - Angle (magnitude) : normalize() sur les deux vecteurs, puis acos(dot()) * 180/PI
   - Signe : simplification choisie (pas de roulis modelise) - comparaison des composantes Z
     des vecteurs normalises plutot qu'un vrai axe lateral (voir idee reportee ci-dessous) :
     fVec.getZ() < vVec.getZ() -> nez plus bas que la trajectoire -> AOA negatif
   - computeAOA() appelee dans simLoop() AVANT computeVerticalSpeed() (important : Cl dans
     computeVerticalSpeed() a besoin de l'AOA fraiche de ce tick, pas celle du tick precedent).
     Consequence acceptee : l'AOA de ce tick utilise le _verticalSpeed du tick precedent
     (computeVerticalSpeed() n'a pas encore tourne) - meme genre de decalage Euler que le
     reste de la boucle, pas juge bloquant pour l'instant
   - Cl derive lineairement de l'AOA : Cl = _liftCoef * AOA (pas de Cl0/ordonnee a l'origine),
     branche directement dans F_LIFT (Simulator::computeVerticalSpeed())
   - Consequence a garder en tete : _liftCoef n'est plus un Cl constant, c'est desormais une
     pente (Cl par degre d'AOA) - la valeur actuelle dans main.cpp (calibree comme un Cl fixe
     genre ~0.03) n'a plus le bon ordre de grandeur pour ce role, a recalibrer en meme temps
     que les autres valeurs bidon de main.cpp (masse/surface, voir etape 2/README)

3bis. Ameliorations futures identifiees, pas urgentes
   - AOA : remplacer la comparaison des Z (approximation) par un vrai angle signe via
     atan2((a x b)·n, a·b) avec un axe lateral de reference n (necessite un getRight() sur
     AirplaneState, ou cross(forward, up)) - plus robuste si le roulis est modelise un jour
   - Decorreler l'AOA/Cl d'un roulis nul : actuellement la simplification suppose implicitement
     qu'on reste dans un plan vertical (pas de sideslip/angle beta modelise)

4. [FAIT] Orientation 3D (quaternion)
   - Quaternion/Vec3 complets, branches dans AirplaneState : _heading (double) remplace
     par _orientation (Quaternion)
   - AirplaneState::getForward() ajoutee (rotate((0,1,0)) sur _orientation)
   - AirplaneState::getHeading() conservee pour l'affichage mais desormais derivee
     (atan2 sur getForward()) plutot que stockee
   - Simulator::simLoop() utilise getForward() au lieu de cos/sin(heading) pour xPos/yPos
   - Bug rencontre et corrige : Quaternion avait un constructeur de copie custom sans
     operator= associe (regle des trois, -Werror=deprecated-copy) -> operator= ajoute

5. Forces vectorielles
   - Actuellement tout est scalaire (F_DRAG, F_NETTE le long d'un seul axe)
   - Passer a des forces en Vec3 (poussee, trainee, portance, gravite) sommees puis divisees par la masse
   - Integrer acceleration -> vitesse -> position en Vec3 plutot qu'en x/y/altitude separes

6. Dynamique angulaire
   - Pas de vitesse angulaire, pas de moment d'inertie, pas de couple
   - A prevoir pour coupler les gouvernes (elevateur/ailerons/rudder) a la rotation du quaternion

7. Divers / a surveiller
   - Comportement au sol : sans portance suffisante, l'avion "tombe" indefiniment, pas de plancher gere
   - Integration Euler simple actuellement (a suffisant pour l'instant, RK4 a envisager plus tard si instabilite)
